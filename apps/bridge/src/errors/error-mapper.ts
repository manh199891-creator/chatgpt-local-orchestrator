import { JobStoreError } from '@local-orchestrator/orchestrator';
import { ProjectError } from '@local-orchestrator/projects';
import { ApiError } from './api-error.js';
const statuses: Record<string, number> = { APPROVAL_REQUIRED:409, WORKTREE_EXISTS:409, WORKTREE_FAILED:500, JOB_NOT_FOUND:404, PLAN_NOT_FOUND:404, JOB_ALREADY_EXISTS:409, PLAN_ALREADY_EXISTS:409, INVALID_TRANSITION:409, JOB_LOCKED:409, FIX_ROUND_LIMIT_EXCEEDED:409, INVALID_JOB_RECORD:500, JOB_STATE_CORRUPTED:500, JOB_EVENT_LOG_CORRUPTED:500, PROJECT_BINDING_CORRUPTED:500, PLAN_STORAGE_CORRUPTED:500, JOB_DATA_INCONSISTENT:500, PLAN_STORAGE_WRITE_FAILED:500, INVALID_PROJECT_ID:400, INVALID_PROJECT_DEFINITION:400, PROJECT_NOT_FOUND:404, PROJECT_ALREADY_EXISTS:409, PROJECT_REGISTRY_LOCKED:409, PROJECT_ROOTS_NOT_CONFIGURED:503, PROJECT_STORAGE_CORRUPTED:500, PROJECT_STORAGE_WRITE_FAILED:500, PROJECT_PATH_OUTSIDE_ALLOWED_ROOTS:400, PROJECT_BINDING_MISSING:409, PROJECT_CONFIGURATION_CHANGED:409, PROJECT_PREFLIGHT_FAILED:409, PROJECT_IN_USE:409 };
export function mapError(x: unknown) { if (x instanceof ApiError) return x; if (x instanceof ProjectError) return new ApiError(x.code, x.message, statuses[x.code] ?? 500); if (x instanceof JobStoreError) return new ApiError(x.code, x.code === 'JOB_NOT_FOUND' ? 'The requested job was not found.' : x.code === 'PROJECT_BINDING_CORRUPTED' ? 'The job project binding is corrupted.' : 'The job data could not be processed.', statuses[x.code] ?? 500); return new ApiError('INTERNAL_ERROR', 'An internal error occurred.', 500); }
export function payload(x: ApiError) { return { success: false, error: { code: x.code, message: x.message, ...(x.details === undefined ? {} : { details: x.details }) } }; }
