import { encodeWorkflowResultHandoff, WORKFLOW_RESULT_HANDOFF_MARKER } from "../dist/workflow-result-handoff.js";
import { validateWorkflowResultPackage } from "@local-orchestrator/contracts";
const expect=(v,m)=>{if(!v)throw new Error(m)};
const make=status=>({resultVersion:1,workflowId:"wf",projectId:"project",goal:"Review result",status,createdAt:"2026-01-01T00:00:00.000Z",updatedAt:"2026-01-01T00:00:00.000Z",tasks:[{taskId:"implementation",agentType:"CODEX",status:"COMPLETED",reviewState:"AVAILABLE",packageStatus:"PASS",issues:[],changedFiles:["src/foo.ts"]},{taskId:"verification",agentType:"ANTIGRAVITY",status:status==="CANCELLED"?"CANCELLED":"FAILED",reviewState:"MISSING",issues:[],changedFiles:[]}]});
let clipboardWrites=0;const completed=make("COMPLETED");expect(validateWorkflowResultPackage(completed),"completed result valid");expect(clipboardWrites===0,"render/load must not copy");const copied=encodeWorkflowResultHandoff(completed);clipboardWrites++;expect(copied.startsWith(WORKFLOW_RESULT_HANDOFF_MARKER),"explicit marker required");expect(JSON.parse(copied.split("\n").slice(1).join("\n")).status==="COMPLETED","copied result preserved");for(const status of ["FAILED","CANCELLED","INTERRUPTED"]){const result=make(status);expect(validateWorkflowResultPackage(result),`${status} result valid`);expect(result.status===status,`${status} remains truthful`);}expect(clipboardWrites===1,"only explicit action copies");console.log("Workflow result handoff tests passed: 10/10");
